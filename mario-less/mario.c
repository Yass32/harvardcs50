#include <cs50.h>
#include <stdio.h>

int pyramid(void);

int main(void)
{
    int s;
    do
    {
        s = get_int("Height: ") ;
    }
    while( 1 >= s && s <= 8 ) ;
        //printf(pyramid) ;

    int pyramid(void);
    {
        for(int i = 0; i < s; i++)
        {
            for(int j = 0; j < s; j++)
                printf("#\n") ;
        }
    }

}